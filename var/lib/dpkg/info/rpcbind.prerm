#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ -x "/etc/init.d/rpcbind" ] || [ -e "/etc/init/rpcbind.conf" ]; then
	invoke-rc.d rpcbind stop || exit $?
fi
# End automatically added section
