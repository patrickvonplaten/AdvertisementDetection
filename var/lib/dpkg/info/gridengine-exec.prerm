#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ -x "/etc/init.d/gridengine-exec" ] || [ -e "/etc/init/gridengine-exec.conf" ]; then
	invoke-rc.d gridengine-exec stop || exit $?
fi
# End automatically added section
